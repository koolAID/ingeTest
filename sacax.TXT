#include<stdio.h>
main()
{
   int p,y,a,b;
   float x;
   clrscr();
   printf("ingrese a\n ");
   scanf("%i",&a);
   printf("ingrese b\n ");
   scanf("%i",&b);
   printf("ingrese p\n ");
   scanf("%i",&p);
   for(y=1;y<p;y++)
   {
	x=(b+p*y)/a;
	if(((b+p*y)%a)==0)
	{
		printf("%f\n",x);
		printf("%i",y);
	}
   }
   getche();
}